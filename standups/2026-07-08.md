# Standup — 2026-07-08

_Generated locally from git history + the Backlog.md board. Window: last 24h._

## Snapshot
- **Backlog:** 93 To Do · 0 In Progress · **12 Blocked** · 92 Done (+108 in `completed/`)
- **CI:** green — last 5 `main` runs all passed (latest 4m45s)
- **Coverage:** 67.9% line (floor now 65%, raised from 60%)
- **Activity:** ~150 commits in 24h across ~100 tasks; unit suite at 1185 tests

## Completed (last 24h)
A large stabilise-and-verify push. By theme:

- **Stability & recovery** — DB open-failure diagnosis + recovery screen (TASK-192), crash-restart simulation harness (TASK-194), hostile-input corpus now covering all 8 persisted decoders (TASK-193/250/255), weekly-gated `quick_check` + backup pruning (TASK-254).
- **Safety guards** — NaN/Infinity guards across the dose chain incl. the reading-explainer sibling (TASK-190/247), read-only-pump architecture guard hardened to catch `sendCommand`/FQN writes (TASK-41/243), sensitivity model gated on the deployed constrained fit (TASK-244).
- **Pump lifecycle & pairing** — `onDestroy` BLE/Garmin teardown made idempotent (TASK-202/262), pairing-window + bonding-phase timeouts (TASK-33/278), timeout field made atomic — not just `@Volatile` (TASK-277/290), tappable service notifications (TASK-203).
- **Security** — model-download auth token stripped on cross-host **and** HTTPS→HTTP-downgrade redirects (TASK-246/300); Gradle wrapper regenerated with a checksum-verified jar + CI wrapper-validation (TASK-299).
- **CI pipeline** — split into parallel jobs, 4-way test sharding via `--total-shards`, `build_runner` cache, network-step retries + timeouts, and hardening so it can't mask a native flake or drop a shard (TASK-287/288/297/298/301).
- **Test quality** — three previously-hollow tests made real (TASK-257→293, 270, 248), whole suite reorganised into feature folders with TASK-refs stripped (TASK-276), widget staleness-alarm freeze-on-unpair fixed (TASK-236→284).
- **Features/UI** — exact-alarm grant prompt (TASK-239), system-health surface (TASK-201), Garmin per-target health so one installed app can't flap the row red (TASK-264), Control-IQ state mapping on `PumpSnapshot` (TASK-126), several overflow/scroll fixes (TASK-280/294/285).

## Issues / Blockers (12)
Nearly all are **final ACs gated on hardware/emulator this session can't provide** — the code half is done and verified; only on-device confirmation remains:

- **Real pump needed:** TASK-33 (BLE pairing/bonding on a real t:slim X2).
- **Emulator needed (VM-service connectivity broken here):** TASK-220, 226, 245, 257, 276, 293, 294, 295, 296, 127 — on-device nav/crash/overflow/API-29-30 checks.
- **Product decision needed:** TASK-31 (a real Garmin complication requires standing up a new watch-app product, not extending the widget).

**Root cause:** one device-enabled session (Pixel + pump) would unblock ~10 of these 12 in a single pass. No CI redness and no correctness blockers.

## Next up (lowest-ordinal ready work)
The stabilise band is largely drained; the queue now surfaces foundational feature/architecture work:

1. **TASK-2** — Predictor models insulin effect from net insulin (EGP baseline) `[HIGH]`
2. **TASK-37** — Decouple aliveness from the widget tree → unblocks **TASK-14** (alerts survive engine death) `[HIGH]`
3. **TASK-3** — Autotune & TOD sensitivity compare like-for-like `[HIGH]`
4. **TASK-228** — Edge-to-edge readiness (targetSdk 37; Android 15+ enforces it) `[HIGH]`
5. Architecture cleanup ready to pick up: **TASK-28/35** (split `providers.dart` + `PersistedStateNotifier`), **TASK-36** (KvStore behind DI seam), **TASK-39** (inject the clock).

Pick from the lowest ordinal whose dependencies are met; new tickets from the hourly review loop land in the stabilise band and jump ahead of these.

---
_Also open from the review loop: TASK-302 (range-clamp persisted decoder numerics, esp. AlertThresholds — safety-adjacent) is the freshest correctness follow-up._
